git clone https://github.com/Leo5700/numchar.git
cd numchar

git config --global user.email "you@example.com"
git config --global user.name "Your Name"

git pull origin
git add .
git commit -a -m "my comment" (--amend)
git push -u origin master (--force)
